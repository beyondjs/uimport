module.exports = function (code, dependencies) {
    // let {exports, reexports} = require('cjs-module-lexer').parse(code);

    // const wrapped = require('./code-wrapper')(code);
    // console.log(exports, reexports);
    return {code};
}
